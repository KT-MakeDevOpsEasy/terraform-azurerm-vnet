# Changelog

## v1.0.0

### Features

- Azure Virtual Network with configurable address space
- Dynamic subnet creation via map variable
- Per-subnet Network Security Groups with flexible rule definitions
- Optional DDoS protection plan association
- Custom DNS server support
- Service endpoints and private endpoint network policies per subnet
- Input validation on VNET name and address space
- Terraform native tests (plan-mode, no credentials required)
